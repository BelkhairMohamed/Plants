# Set default index file
DirectoryIndex index.php

# Prevent directory listing
Options -Indexes

# Rewrite rules for cleaner URLs (optional)
# Uncomment if you want to use clean URLs instead of query strings

# RewriteEngine On
# RewriteBase /JobinTech/PHP/Plants/
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)$ public/index.php?controller=$1 [QSA,L]

# For now, we use query string routing, so this file is optional

